/**
 * Pivot config
 * @description Interface for Pivot config
 */
export interface IPivotConfig {

    /**
     * Nodes expanded as default
     * @description Whether nodes should be generated
     * with the expanded flag active
     */
    nodesExpandedAsDefault?: boolean;
}